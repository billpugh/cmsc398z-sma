# FastAPI Configuration

# SECRET_KEY - REQUIRED for JWT token signing
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# NEVER use a predictable value! This protects JWT tokens from forgery.
# See SETUP-AUTHENTICATION.md#phase-2-considerations-jwt-tokens
SECRET_KEY=REPLACE-WITH-OUTPUT-FROM-SECRETS-TOKEN-HEX-32

# JWT Settings (for production JWT auth)
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database
# RECOMMENDED: Reuse Phase 1 database to see same users/posts in your API
DATABASE_URL=sqlite:///../../../shared/database/app.db
# OR create a fresh database for Phase 2:
# DATABASE_URL=sqlite:///./phase2.db

# CORS - Origins allowed to access the API
# Development: Include your frontend dev server
# Production: Replace with your deployed frontend URL
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Application Settings
APP_NAME=Social Media API
API_VERSION=v1

# ============================================================================
# AUTHENTICATION MODE (see SETUP-AUTHENTICATION.md)
# ============================================================================
#
# Development Mode: No special env vars needed (use dev-login endpoint)
# Production Mode: SECRET_KEY above is used for JWT signing
