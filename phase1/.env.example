# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development

# SECRET_KEY - REQUIRED FOR ALL MODES
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# NEVER use a predictable value! This protects session cookies from tampering.
# See SETUP-AUTHENTICATION.md for details.
SECRET_KEY=REPLACE-WITH-OUTPUT-FROM-SECRETS-TOKEN-HEX-32

# Database
# NOTE: Database path should be configured in app.py using absolute paths with pathlib
# Do NOT use relative paths here - they can cause "unable to open database file" errors
# See CLAUDE.md and TODO-phase1.md Task 3.2 for the correct configuration pattern
# DATABASE_URL is NOT needed in .env - configure directly in app.py instead

# ============================================================================
# AUTHENTICATION OPTIONS (Choose ONE - see SETUP-AUTHENTICATION.md)
# ============================================================================

# Option 1: Development Mode (No Auth)
# No additional configuration needed - just use dev_login() in your code
# WARNING: Never deploy this publicly!

# Option 2: Google OAuth (Recommended for Production)
# Setup guide: SETUP-AUTHENTICATION.md#option-2-google-oauth-recommended
# Get credentials from: https://console.cloud.google.com/
# GOOGLE_CLIENT_ID=<your-client-id>.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=<your-google-client-secret>

# Option 3: Email Magic Links (Advanced)
# Setup guide: SETUP-AUTHENTICATION.md#option-3-email-magic-links-advanced
#
# For Gmail App Password (Development Only):
#   1. Enable 2FA: https://myaccount.google.com/security
#   2. Generate App Password: https://myaccount.google.com/apppasswords
#   3. WARNING: MAIL_PASSWORD is NOT your Gmail password - use App Password!
#
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=<your-16-char-app-password-here>
# MAIL_DEFAULT_SENDER=your-email@gmail.com
#
# For SendGrid (Production Recommended):
#   1. Sign up: https://signup.sendgrid.com/
#   2. Create API key with "Mail Send" permissions
#   3. Verify sender email
#
# SENDGRID_API_KEY=<your-sendgrid-api-key-here>
# MAIL_DEFAULT_SENDER=your-verified-email@gmail.com

# Application Settings
APP_NAME=Social Media App
MAX_CONTENT_LENGTH=16777216  # 16MB max file upload size
